bar.show_data_label(fmt=optional,thresh_hold=optional)
    fmt:
    thresh_hold:

bar.set_theme(pal=optional,tone=optional)
    pal:
    tone:


testing
    can set on each unit
    multiple dataset for each type
        <5 dim
        5,10 dim

dataset
    sorting


todo
    test-can set on each unit
    test-multi dataset for each type
    dataset-sort
    dataset-distinct count
    dataset-ratio
    dataset-average
    style-fonts theme

    stackbar-set color data label, within bar
    stackbar-show total value along dimension axis
