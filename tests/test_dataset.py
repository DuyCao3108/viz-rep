"""Dataset.query(): unfiltered, filtered, legend pivot, unknown dim/measure ->
ValueError, agg variants. Stub only — fill in as Dataset's behavior stabilizes."""


def test_placeholder(sample_dataset):
    assert sample_dataset is not None
